const db = require('../../models');
exports.list = async(req,res,next)=>{ try{ const where={}; if(req.query.companyId||req.user.companyId) where.companyId=req.query.companyId||req.user.companyId; const list=await db.Sop.findAll({where, order:[['created_at','DESC']]}); res.json({ok:true,data:{sops:list}});}catch(e){next(e);} };
exports.create = async(req,res,next)=>{ try{ const companyId=req.body.companyId||req.user.companyId; if(!req.body.title) return res.status(400).json({ok:false,error:{code:'VALIDATION_ERROR'}}); const s=await db.Sop.create({ companyId, ...req.body }); res.status(201).json({ok:true,data:{sop:s}});}catch(e){next(e);} };
exports.getOne = async(req,res,next)=>{ try{ const s=await db.Sop.findByPk(req.params.id); if(!s) return res.status(404).json({ok:false,error:{code:'NOT_FOUND'}}); res.json({ok:true,data:{sop:s}});}catch(e){next(e);} };
exports.update = async(req,res,next)=>{ try{ const s=await db.Sop.findByPk(req.params.id); if(!s) return res.status(404).json({ok:false,error:{code:'NOT_FOUND'}}); Object.assign(s, req.body); await s.save(); res.json({ok:true,data:{sop:s}});}catch(e){next(e);} };
exports.remove = async(req,res,next)=>{ try{ const s=await db.Sop.findByPk(req.params.id); if(!s) return res.status(404).json({ok:false,error:{code:'NOT_FOUND'}}); await s.destroy(); res.json({ok:true,data:{message:'Deleted'}});}catch(e){next(e);} };
