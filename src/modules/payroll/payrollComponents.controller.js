const db = require('../../models');
exports.list = async(req,res,next)=>{ try{ const companyId=req.query.companyId||req.user.companyId; const where={}; if(companyId) where.companyId=companyId; if(req.query.type) where.type=req.query.type; const list=await db.PayrollComponent.findAll({where, order:[['name','ASC']]}); res.json({ok:true,data:{payrollComponents:list}});}catch(e){next(e);} };
exports.create = async(req,res,next)=>{ try{ const companyId=req.body.companyId||req.user.companyId; if(!req.body.name) return res.status(400).json({ok:false,error:{code:'VALIDATION_ERROR'}}); const pc=await db.PayrollComponent.create({ companyId, name:req.body.name, code:req.body.code, type:req.body.type||'earning', amount:req.body.amount||0, description:req.body.description, formula:req.body.formula, isTaxable:req.body.isTaxable, isFixed:req.body.isFixed }); res.status(201).json({ok:true,data:{payrollComponent:pc}});}catch(e){next(e);} };
exports.update = async(req,res,next)=>{ try{ const pc=await db.PayrollComponent.findByPk(req.params.id); if(!pc) return res.status(404).json({ok:false,error:{code:'NOT_FOUND'}}); Object.assign(pc, req.body); await pc.save(); res.json({ok:true,data:{payrollComponent:pc}});}catch(e){next(e);} };
exports.remove = async(req,res,next)=>{ try{ const pc=await db.PayrollComponent.findByPk(req.params.id); if(!pc) return res.status(404).json({ok:false,error:{code:'NOT_FOUND'}}); await pc.destroy(); res.json({ok:true,data:{message:'Deleted'}});}catch(e){next(e);} };
